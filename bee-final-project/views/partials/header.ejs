<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Hospital Website</title>
    <link rel="shortcut icon" href="/image/heartbeat-solid.svg" type="image/x-icon">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css"/>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .navbar {
            display: flex;
            align-items: center;
            flex-wrap: wrap;
        }
        
        .nav-links {
            display: flex;
            align-items: center;
            flex-wrap: wrap;
        }
        
        .auth-buttons {
            display: flex;
            align-items: center;
            margin-left: 15px;
        }
        
        .nav-button {
            padding: 8px 15px;
            border-radius: 5px;
            text-decoration: none;
            font-weight: bold;
            font-size: 1.7rem;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
        }
        
        .login-btn {
            background: linear-gradient(to right, #32cd32, #16a085);
            color: white;
        }
        
        .login-btn:hover {
            background: linear-gradient(to right, #2bb32b, #138a72);
        }
        
        .logout-btn {
            background: #16a085;
            color: white;
            margin-left: 10px;
        }
        
        .logout-btn:hover {
            background: #138a72;
        }
        
        .user-info {
            display: flex;
            align-items: center;
            color: #16a085;
            font-weight: bold;
            font-size: 1.7rem;
        }
        
        .user-info i {
            margin-right: 5px;
        }

        /* Make navbar responsive */
        @media (max-width: 991px) {
            .navbar {
                flex-direction: column;
                align-items: flex-start;
            }
            
            .auth-buttons {
                margin-left: 0;
                margin-top: 10px;
            }
        }
    </style>
</head>
<body>
    
        <div class="header">
            <a href="/" class="logo"><i class="fas fa-heartbeat"></i>HealthScape</a>
            <nav class="navbar">
                <div class="nav-links">
                    <a href="/">home</a>
                    <a href="/services">services</a>
                    <a href="/about">about</a>
                    
                    <a href="#" id="book-appointment-link">book</a>
                    <a href="/review&blogs">review&blogs</a>
                </div>
                
                <div class="auth-buttons">
                    <% if (typeof isLoggedIn !== 'undefined' && isLoggedIn) { %>
                        <div class="user-info">
                            <i class="fas fa-user-circle"></i> 
                            <%= typeof username !== 'undefined' ? username : 'User' %>
                        </div>
                        <a href="/logout" class="nav-button logout-btn">
                            <i class="fas fa-sign-out-alt"></i> Logout
                        </a>
                    <% } else { %>
                        <a href="/login" class="nav-button login-btn">
                            <i class="fas fa-sign-in-alt"></i> Login
                        </a>
                    <% } %>
                </div>
            </nav>
            <div id="menu-btn" class="fas fa-bars"></div>
        </div>
        <br><br><br><br><br>
    
    <main>
