<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registration Successful - HealthScape System</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="styles.css">
    <style>
        /* General Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            background: linear-gradient(45deg, #16a085, #2ecc71);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .success-container {
            background-color: rgba(255, 255, 255, 0.9);
            padding: 40px;
            border-radius: 8px;
            width: 100%;
            max-width: 450px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }

        .heading {
            font-size: 32px;
            color: #16a085;
            margin-bottom: 10px;
        }

        .subheading {
            font-size: 18px;
            color: #555;
            margin-bottom: 20px;
        }

        .success-icon {
            font-size: 80px;
            color: #27ae60;
            margin-bottom: 20px;
        }

        .message {
            padding: 20px;
            background-color: #f8f9fa;
            border-radius: 8px;
            margin-bottom: 25px;
            font-size: 16px;
            line-height: 1.5;
            color: #333;
        }

        .user-details {
            margin: 15px 0;
            padding: 15px;
            background-color: #eaf7f0;
            border-radius: 8px;
            text-align: left;
        }

        .user-details p {
            margin: 5px 0;
            font-size: 16px;
        }

        .btn {
            background-color: #16a085;
            color: white;
            padding: 12px 25px;
            border: none;
            border-radius: 4px;
            font-size: 18px;
            cursor: pointer;
            transition: background-color 0.3s ease-in-out;
            text-decoration: none;
            display: inline-block;
            margin-top: 10px;
        }

        .btn:hover {
            background-color: #1abc9c;
        }

        .timer {
            margin: 20px 0;
            font-size: 14px;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="success-container">
        <i class="fas fa-check-circle success-icon"></i>
        <h1 class="heading">Registration Successful!</h1>
        <p class="subheading">Your account has been created</p>
        
        <div class="message">
            <%= message %>
        </div>
        
        <div class="user-details">
            <p><strong>Username:</strong> <%= username %></p>
            <p><strong>Role:</strong> <%= role %></p>
        </div>
        
        <p class="timer">You will be redirected to login page in <span id="countdown">5</span> seconds.</p>
        
        <a href="/login<%= redirect ? '?redirect=' + redirect : '' %>" class="btn">Login Now</a>
    </div>

    <script>
        // Countdown timer for redirect
        let seconds = 5;
        const countdownElement = document.getElementById('countdown');
        const urlParams = new URLSearchParams(window.location.search);
        const redirect = urlParams.get('redirect');
        const redirectUrl = redirect ? `/login?redirect=${redirect}` : '/login';
        
        const interval = setInterval(() => {
            seconds--;
            countdownElement.textContent = seconds;
            
            if (seconds <= 0) {
                clearInterval(interval);
                window.location.href = redirectUrl;
            }
        }, 1000);
    </script>
</body>
</html>